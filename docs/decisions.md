# 决策记录

## 2026-09-18：正式开工 / M0

基线为任务书 v1.1 已确定行为；保留原材料，旧实验路径不沿用。已成功读取两次审视会话：

- `01a0b3ad-4dc3-7023-b559-d1678d0e7b07`：审视Dota2专服开发计划书。
- `01a0b3b2-9277-7870-8d70-7c79cd73ce58`：审视Dota2专服任务书（含后续合并结论）。

本记录将建议落实为任务，不将备选实现视作已确定需求。

| 项目 | 决策 / 当前约束 | 后续验证 |
| --- | --- | --- |
| 仓库 | 使用现有目录及 main，本地 Git；不创建远端、不指定许可证 | M0-01 基线提交 `3bdd52b` |
| Go | 先用已安装 Go 1.22.2 开发无第三方依赖的 M0 工具；不是发布工具链承诺 | M0-06 根据当时受支持版本和双平台结果固定工具链；当前版本较旧，发布前必须复核 |
| module | 临时使用 `d2core.local/core`，不占用或暗示任何 GitHub owner | 远端确定后统一改 module/import 路径 |
| 工具边界 | `m0-inspect` 只读取显式路径、解析链接并计算文件指纹；不执行模板、不扫描安装、不探测端口、不验证写权限 | M0-02 实机输入齐全后采集；指纹不能证明资源默认未修改，也不能冻结并发写入的资源 |
| 日志实验 | 先验证将测试子进程输出直接指向文件、父进程退出后继续输出的可行性 | 不以测试进程替代引擎 `-con_logfile`、强制杀父进程或 Ubuntu 证据 |
| 实机启动 | 必须先记录允许修改的安装/cfg、测试目录、端口及禁止触碰对象 | 缺少隔离输入时不启动 Dota、不修改 gameinfo、不占用候选游戏端口 |

## 审视意见进入阶段的检查与验收

| 事项 | 对应阶段 | 必交付检查 / 验收 | 当前状态 |
| --- | --- | --- | --- |
| 最终生效快照 | M1-01—03 | 保存模板与受支持 CLI 覆盖合并结果；每代重新展开 cfg/log 路径；修改模板后重启仍保留覆盖值 | 已确定该约束，未实现 |
| 清理失败重试 | M1-01、06 / M2 | 固定活动列表归属、端口释放条件、重试触发与退避、停止历史结果和最新清理状态；测试只重试有归属的待清理文件，不能重新启动进程 | 方案待 M1 固定；未选择“后台重试”或“重复 stop 触发”备选 |
| 创建请求去重 | M1-01 / M2 | 在 API 实现前固定字段、必填性、作用域、同键不同内容冲突及保留规则；M2 验证并发、崩溃、回收后旧键重试 | 契约提前到 M1，持久化可靠性仍 M2 |
| 操作恢复 | M0-05 / M2 | M0 调查进程已创建但身份未落盘窗口；M2 制作“中断阶段—恢复动作—操作终态”表，明确离线时间是否计入超时 | 未核验身份前禁止派生下一代；其他策略未定 |
| 附加端口 | M0-04、06 / M1-01 / M3 | 按 PID/进程树记录 TCP/UDP 地址、监听和临时端口；若必需附加端口影响单实例，M1 固定表达与显式检查，M3 自动分配 | 不预设只有 game_port，也不提前添加模板字段 |
| 离线日志 | M0-05 / M3 | 正常退出、崩溃后引擎继续写本代日志；恢复可读；M3 明确管理端离线时容量策略边界 | Windows 测试进程部分验证，Dota/Ubuntu 未验证 |
| 互斥与串行 | M1-05 / M2 | M1 即实现单 data-dir 排他写入和最小串行；M2 完善故障可靠性 | 未实现 |
| 静态 check 边界 | M1-01、02 | 不声称理解任意 cfg 中所有 VPK/exec 引用；静态成功不代表可加载 | 待正式契约 |
| A2S | M4 | 使用可选，交付必做；普通启动不得隐式修改 gameinfo | M0 不依赖 A2S |
| 共享安装小实验 | M0 输入允许时 / M3 必测 | 调查 cfg 以外共享写入冲突；完整双实例验收留在 M3 | 可选提前，不阻塞当前输入准备 |

M0 没有达到出口，不开始 M1 管理架构。实际产品行为变化、平台支持缩减及重要运维取舍需向用户说明后确认。

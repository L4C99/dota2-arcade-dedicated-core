# M2-06 整机重启验收（双平台通过）

本文记录 [M2](m2.md) 整机重启流程及实测结果；双平台最终结果见文末；准备阶段记录按时间保留。Windows 10 与 Ubuntu 24 分别执行；管理端重启、模拟 boot ID、辅助进程测试或编译不能替代真实操作系统重启。普通重启也不等同断电测试。

## 环境与证据

只使用明确允许重启的隔离机器。执行前核对主机身份、操作系统版本、CPU/内存/磁盘、当前进程与服务、游戏安装、可写目录及端口预约；不得使用生产主机。记录本次允许影响的目录、进程与重启范围。新机器使用独立 SSH 身份与已核验主机密钥，不复制生产运行记录、不复用生产 data-dir。

准备相同提交的核心、游戏及依赖、用户提供的原始 VPK。记录二进制/地图指纹和游戏版本；地图的客户端名称与资源匹配。核心不负责安装或更新游戏。新环境先完成 check、真实启动、客户端进房和正常回收，确认运行前提后再进行重启用例。

管理端不设置自动启动，测试游戏也不能由其他启动器或服务自动拉起。使用普通测试用户、独立 data-dir、独立生成cfg及日志目录；明确收集并保留重启前后状态与操作结果，不修改原记录来制造旧PID冲突。

## 实际操作与判定

1. 创建独立测试实例并等待就绪，客户端确认进入。记录实例ID、创建幂等键、操作ID、代次、完整进程身份、配置快照、日志指纹和监听。另保存管理端离线的基线副本作为证据，不覆盖活动目录。
2. 正常退出管理端，确认游戏仍存活；保存最后的游戏身份。执行已授权的隔离机操作系统重启，保存重启前后系统启动标识/时间。不要先 stop 回收该实例，否则无法验收残留活动记录。
3. 系统回来后，先只读检查：原游戏不应重新出现，核心不得由未声明的自启动项运行，原 data-dir 必须保留。保存当前进程与监听清单，不能把旧PID数字视作身份。
4. 使用同一用户、同一 data-dir 启动核心。查询 list/status/operation，确认旧实例ID、快照、代次及历史操作仍在；当前状态不得沿用旧 ready，不得产生新一代或新游戏进程。旧PID不存在时应反映已退出；若被无关进程占用或身份不可核验，则应拒绝接管并保留 unknown/诊断。
5. 使用原模板路径、原端口和原创建键重试，必须返回原实例/操作，不创建替代房间。复核游戏进程与代次没有增加。不能通过换创建键绕过未确认的残留。
6. 显式 stop 原实例，核对确认退出后才能回收。若身份不可确认，应保持失败和分配，保留证据，不手工杀旧PID或删记录继续。核对无关进程未被控制、生成文件按归属清理，原VPK/模板和历史日志保留。
7. 旧实例成功回收后，以新意图创建新实例，实际进房并正常停止。保存操作终态、列表、端口及文件回收证据，最后停止本轮管理端。

记录每一步命令、返回值和时间；原始记录留在私有验收目录，公开报告仅保留脱敏结论。若真实重启未自然产生PID复用，明确记录“未观察到实际PID复用”；既有身份冲突辅助测试单独列示，不能声称实测覆盖了该场景。任何失败均保留，不以删状态、重建 data-dir 或修改校验和绕过。

## 当前准备状态

2026-09-20：用户已提供并授权专用 Windows 10 虚拟机，包括虚拟机内测试账户权限调整和整机重启，明确禁止影响宿主机。SSH 主机指纹已核验；开发包及原始地图哈希一致。普通测试用户已实际启动地图，当前等待用户进房确认；尚未执行整机重启。前两次因新用户 Steam SDK 安装路径缺失失败，均保留诊断并正常回收；补齐该测试用户既有 Steam DLL 路径后地图就绪，网络认证仍须实际连接验证。所有系统设置变更限于虚拟机，原始证据及恢复清单在私有 local/validation/win10-vm。Ubuntu 24 可重启环境仍待提供。M2-06 保持未验证，M2整体未通过，最终交付仍须补测通过。

后续连接结果：客户端超时；服务端已收到连接请求，但因未连接 Steam、缺少服务器身份而拒绝握手。虚拟机没有默认网关，Steam DNS 查询亦超时。测试实例已回收、空闲管理任务已停止，等待隔离虚拟机外网连接；不能把地图就绪记作进房或整机重启通过。

## Windows 10 真实重启结果（2026-09-20，通过）

使用独立 Windows 10 虚拟机、普通测试用户、开发包提交4256200及用户原始VPK。先完成真实进房与正常回收，再创建重启实例并由用户确认进房。管理端正常退出0，核验原专服仍存活后才执行虚拟机操作系统重启。启动时间从05:33:49.500 UTC变为06:59:11.500 UTC；宿主机未重启或修改系统配置。

重启后先检查，未发现核心或游戏自启动，旧PID不存在；本轮未自然观察到PID复用，不声称覆盖真实PID复用场景。恢复同一用户、同一data-dir后，旧实例被识别为stopped/failed，旧ready证据失效；实例ID、配置快照、端口、generation=1及一份运行记录保持不变。同键重试返回原实例和原创建操作，没有新增代次。原操作历史succeeded与当前实例failed分别呈现，不改写历史结果。

显式stop后旧记录完整回收，生成cfg清理，历史日志、原地图和模板保留。新意图成功复用端口创建新ID，Steam认证OK、默认VAC启用，用户确认再次正常进房。最后stop confirmed=true/forced=false、cleanup=complete，活动列表为空，TCP/UDP测试端口均释放，管理端正常退出0。

本轮是普通操作系统重启，不是断电测试；身份冲突辅助测试继续独立列示。Windows M2-06通过，Ubuntu整机重启仍待环境，M2整体未通过。私有证据位于local/validation/win10-vm，公开报告不包含玩家原始日志和连接凭据。

Ubuntu补验已于2026-09-20开始准备：用户提供独立 Ubuntu 24.04 测试服务器、经独立核验的主机指纹及重启授权。新解压开发包和原始地图哈希一致，以普通用户运行、游戏只读挂载；Steam运行依赖单独准备，未更新游戏安装。两次隔离配置目录准备错误均保留失败及回收证据，修正后地图就绪、Steam认证OK和默认VAC启用，正在等待首次客户端进房。Ubuntu整机重启尚未执行，不标通过。

## Ubuntu 24 真实重启结果（2026-09-20，通过）

在用户授权的新隔离服务器完成首次部署：Ubuntu24.04.3、游戏版本6934、普通系统用户、独立解压包与data-dir，原始VPK哈希一致。Steam SDK运行依赖在测试目录独立准备；原游戏安装只读挂载，默认cfg复制至独立目录后保留其内容。先前空cfg遮挡默认按键、复制后目录归属错误均作为失败记录保留，分别修复并安全回收，没有改写失败操作为成功。

用户确认基线进房及正常回收后，新建重启实例并再次确认进房。管理端主进程SIGINT正常退出0，专服路径、boot ID和启动tick再次核验匹配、仍存活；保存离线状态后执行真实操作系统重启。重启后boot ID改变，旧PID不存在，核心/游戏未自动启动、测试端口未监听。本轮未自然观察到PID复用，不宣称真实PID复用验收通过。

同一普通用户、同一data-dir恢复后，旧实例为stopped/failed且旧ready证据无效；配置快照、端口、generation=1和运行记录数量均不变。同键返回原实例与原创建操作，断言没有Dota进程后才显式stop回收旧记录。随后新意图创建新ID，默认VAC和Steam认证OK，用户确认正常进房。最终stop confirmed=true/forced=false、cleanup=complete，cfg与输入FIFO清理、日志与原地图保留，活动列表为空、游戏端口释放；管理端退出0，测试单元关闭，原gameinfo哈希未变。

至此Windows及Ubuntu的M2-06均通过。普通重启证据不代替断电测试；原始记录在私有local/validation/ubuntu-reboot，旧生产服务器与本地主机未受改动。

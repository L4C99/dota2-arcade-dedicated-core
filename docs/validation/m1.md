# M1 实现与验收记录

按原任务编号推进，不以 M0 实验或 CI 代替本阶段真实验收。

| 任务 | 实现状态 | 验证状态 |
| --- | --- | --- |
| M1-01 契约 | 完成，见 local-api.md | 已对照完整任务书审查，补齐状态、证据和端口规则 |
| M1-02 模板 | 完成，离线 check/version 可用 | Windows 测试/vet；Ubuntu 普通用户原生配置测试通过 |
| M1-03 实例记录 | 完成，已接入管理服务 | Windows/Ubuntu 原生风险测试通过 |
| M1-04 进程与观测 | 完成 | 双平台辅助进程及真实 Dota 启动/超时/停止通过 |
| M1-05 本地管理与 CLI | 完成 | 双平台 CLI 实机闭环、整合测试及实际跨用户拒绝通过 |
| M1-06 重启、停止回收 | 完成 | 双平台真实重启/停止/回收及加载中止通过 |
| M1-07 双平台验收 | 完成 | 两平台首次及重启后二次进房均获用户确认；收口见下文 |

下文保留实施过程与失败记录。已完成的实机出口包括两平台 create→查询→客户端进房→restart→再次进房→stop→历史查询→确认移出活动列表/解除分配→新建不同 ID。所有失败项沿用完整任务书；后续逐项记录，不删除未通过检查。

## M1-02 验证（2026-09-19）

Windows：Go 1.27.1，`go test -p 2 ./...`、`go vet ./...` 通过。Ubuntu 24.04.5：以普通游戏用户执行交叉编译后的配置测试二进制，`-test.v -test.timeout=60s` 全部通过；这是本机执行结果，不只交叉编译。测试不启动专服。

覆盖重复/未知/大小写错误 JSON 字段、版本、大小与 null 边界、绝对路径和文件类型、未知/残缺占位符、参数与 cfg 不同转义上下文、带空格路径、ASCII 日志路径、超时/就绪规则、源配置不变。check 的 stdout 是一份 JSON，静态检查失败不会生成运行目录；它不验证地图可加载或端口可用。

该次 M1-02 测试尚未涉及真实引擎；本阶段最终出口见文末。

## M1-03 验证（2026-09-19）

记录模块保存规范化模板、显式端口、随机实例/操作 ID、各代参数与 cfg/log 路径；生成 cfg 独占创建并保留内容指纹，清理核对实例、代次、路径及内容。日志、快照和操作历史不随 cfg 清理删除。创建键重试优先返回原记录，不重新读取修改后的源模板。

Windows 和 Ubuntu 普通用户原生测试通过：同模板双记录、端口预约/实际 TCP 与 UDP 冲突、幂等冲突、修改源模板后的快照复用、代次隔离、外来/已改 cfg 不被删除、清理重试、历史保留、损坏/版本记录拒绝。

首轮测试确实失败：Windows 通配绑定漏检回环占用，缺失 keys 和 null run 被接受，清理接受非 Store 持有的实例。已分别修复独占 socket、零值解码及记录/归属验证，保留失败测试并双平台复测通过。测试可触发 Windows 临时测试程序的防火墙提示；无需放行外部连接。

当前只有文件级保存与基本重试；完整崩溃持久化、互斥与恢复仍分别由 M1-05/M2 验证，不宣称已完成恢复验收。

## M1-04 原生进程验证（2026-09-19）

Windows：持有进程句柄后校验精确创建时间/程序路径；独立辅助进程核验控制台归属后仅发送 quit；强制停止使用该句柄。Linux：setsid、独立 FIFO、boot ID/start ticks/exe/argv/FIFO inode 与 pidfd，拒绝 PID-only 信号；按设备号/inode 清理输入 FIFO。

双平台辅助进程实测身份冲突拒绝、优雅退出/强制停止、原生套接字归属、父进程结束后输出持续写入；Windows 另测父进程非零退出。读取仅针对当前代独占日志，失败信号优先，中文 tail 保真。Linux 首轮出现退出竞态（/proc 信息消失先于观察到 pidfd 退出）；仅在同一 pidfd 上有界重查，保留失败测试后原生三轮通过。

以上测试没有启动 Dota，也不能替代 M1-07。真实引擎的加载超时、停止、重启和客户端进房仍须独立执行。

## M1-07 Windows 实机发现与修复（2026-09-19）

正式 Go 核心已使用同一用户提供的 VPK 完成首次进房、保留实例 ID/端口的第二代启动及再次进房，均由客户端用户确认。停止后历史结果/日志可查、活动列表移除、端口释放、生成 cfg 清理；重复 stop 返回原结果、已回收 restart 拒绝，新建获得不同 ID。

首轮两次停止均走 10 秒超时后的强制结束，**不记为优雅停止通过**。日志暴露控制台标准输入异常：Go os/exec 的默认 Stdin 是 NUL 文件，妨碍引擎使用控制台输入。改为 NULL 标准句柄，保留独立文件 stdout/stderr 和原身份核验；依据 [Microsoft 标准句柄说明](https://learn.microsoft.com/en-us/windows/console/getstdhandle)。修复后真实 Dota 约0.67秒正常退出，confirmed=true、forced=false，日志包含正常 Shutdown。不修改防火墙、gameinfo、客户端参数或地图。

原控制台辅助进程测试存在假通过：提前退出也会使 Quit 返回 nil。现同时要求标准输入为控制台且实际收到 quit 标记；修复后宿主完整 go test/vet 通过。其他测试不能替代真实进房。后续仍须完成双平台剩余失败用例和 Ubuntu M1 闭环，M1 未验收完成。

追加真实 Windows 负例：独立模板设不可达就绪规则和2秒超时，返回 START_TIMEOUT/observe，保留失败记录和分配；引擎尚未建立控制台时确认强制退出，随后显式 stop 回收。另一实例先就绪后 restart，在第二代 process=running/room=loading 时 stop，原 restart 返回 cancelled，stop 成功回收，延后查询仍只有两代且无新进程。测试模板与源模板分开，未改共享 VPK。

## M1-05/06 整合验证（2026-09-19）

Windows 宿主与 Ubuntu 普通用户执行辅助进程整合：创建→就绪→源模板修改→快照重启→读取中文日志→停止→历史查询、幂等重试、移出活动列表及端口释放均通过。另测早退、超时、加载中停止、排队重启被停止中止、历史成功后异常退出、清理失败重试、身份不明不回收、未知 ID 与无效模板、三处保存故障不误报成功、Close 返回后无后台写入。排队取消测试不能替代真实重启中停止，后续单独验收。

IPC：双平台往返、第二管理端拒绝、请求取消后已受理任务继续、报文大小、端点替换不误删、停接收后仍持锁直至显式 Close。Windows 实际 AnonymousToken 访问拒绝；Ubuntu 实际 UID65534 子进程访问拒绝（普通用户测试明确 skip，此项另以 root 在自建临时目录单跑通过，不创建账号）。跨 UID 首轮测试因 os.IsPermission 不识别 net.OpError 包装误报失败，改 errors.Is 后实际 EACCES 得到确认。

数据根和接口目录的权限均核验；已有不安全目录被拒且原权限不变。Windows 严格 ACL 测试在沙箱受限令牌下被拒，因此按正常审批在宿主执行，未放宽 ACL。真实 CLI/命名管道 smoke 验证空 list、第二 serve 拒绝和未知 status；只结束该测试创建的无实例管理端。

审查中发现并修复 Linux 监听协议枚举与核心不一致、Close 提前释放写锁、保存失败错误状态、历史输入清理记账。完整 M2 崩溃恢复及 M3 存储上限仍未实现/验收。

## M1 最终出口（2026-09-19）

**M1 已通过；M2—M4 未完成。** 正式核心实机基线为 `0d328906`，Windows 标准输入修复为 `dab50a0`；最终收口另补同句柄退出复核和两项断言。原始状态、操作 JSON、日志、资源指纹与用户进房确认存放于忽略的 `local/validation/m1/`，不上传玩家信息或机器连接资料。

| 验收 | Windows 10 | Ubuntu 24.04.5 |
| --- | --- | --- |
| CLI check/create/status/operation，首次实际进房 | 通过，用户确认 | 通过，用户确认 |
| restart 保留 ID/端口/快照，旧代退出后第二代进房 | 通过，用户确认 | 通过，用户确认 |
| stop、历史日志/结果、list 移除、端口释放、生成文件清理、新 ID | 通过 | 通过，含 FIFO 清理 |
| 就绪后优雅停止 | 修复后 forced=false，约0.67秒 | 两代 forced=false |
| 真实启动超时、重启第二代加载时 stop | 通过；控制台未建立时允许核验后的强制回退 | 通过；固定 FIFO quit 正常退出 |
| 无效模板、端口冲突、早退、未确认退出不回收、清理失败重试、历史状态、BUSY、未知/已回收 ID | 本机辅助进程/CLI通过 | 本机辅助进程/CLI通过 |

本轮 Windows 引擎为6934/11015183，Ubuntu受保护安装为6933/10999813；不同版本下本次地图连接实际通过，不宣称任意版本兼容。两端使用与 M0 相同 SHA256 的用户 VPK 及唯一 addon 名。Windows 源模板/VPK/gameinfo 指纹未变；Ubuntu 私有挂载将 cfg 与 addon 隔离，未变更生产安装。验收结束两平台无活动测试房间，测试管理端/单元已退出，证据保留。

Ubuntu 限制单元串行运行，整个验收期间内存峰值约811 MiB（含引擎及 cgroup 计量），结束仅管理端约7.3 MiB；这不是容量或性能承诺。管理接口不需要公网防火墙规则。

最后补强测试首次在 Ubuntu 失败：早退恰好发生在读取 `/proc/.../net/udp` 时，被归类为 IO_ERROR。现于日志/绑定读取后复核同一已验证句柄，将确认退出记为 START_FAILED/observe，并使就绪证据失效；不把权限错误或身份不明当作已退出。原断言未删改，修复后原生连续10次通过，Ubuntu 全部 core 测试、Windows core/engine 与 vet 通过。BUSY 明确不增加操作/代次；早退明确保留双 ID、错误阶段、诊断文件和端口预约。

M1 仅覆盖正常管理期间的闭环。管理端崩溃完整恢复、持久化故障窗口、自动端口、多实例容量与离线日志策略、交付包和 A2S 仍按原任务书继续，不能把本次结果视为首版交付完成。

# Dota 2 游廊专服核心：开工任务书

开工基线 v1.1 · 2026-09-18

本文是新项目从零开发的独立输入，当前尚无 Go 实现。正文行为约束作为开工基线；技术选型与数值由指定任务验证后记录，不要求开工前全部确定。历史实验仅作为复验线索，不能标记为已验收。

## 1. 目标与范围

开发本机专服管理核心 `d2core`，供地图作者、启动器和管理系统使用。采用 Go，最终提供 Windows/Linux 可执行文件，使用者无需安装 Go。仓库暂名 `dota2-arcade-dedicated-core`。

首个开发里程碑是：使用一个真实地图模板，完成创建、启动、客户端进房、停止和同配置重启。先证明引擎与管理流程可行，再扩展恢复、多实例和双平台交付。

| 责任方 | 范围 |
| --- | --- |
| 核心 | 实例记录、进程控制、配置生成、端口管理、日志、状态和管理恢复 |
| 使用者或上层程序 | 准备游戏与地图资源、选择模板、启动客户端、分配玩家、决定何时回收房间 |
| 地图 | 游戏内数据与玩法流程 |
| 后续管理系统 | Web、远程通信、节点管理和跨机调度 |

资源下载与更新、匹配、玩家账号管理不纳入当前核心范围。核心提供受访问限制的本地协议和 CLI；不开放远程管理接口。

**已确认：**正常情况下，开发过程同步测试 Windows/Linux；开发期以手动 CLI 测试为主，完成后作为联机启动器的核心。测试地图由用户提供，不预设地图名单。双平台验证贯穿各阶段，不留到交付前集中补验。测试环境为本地 Windows 10（专服与客户端同机）和云服务器 Ubuntu（运行专服）。

## 2. 必须保留的行为约束

### 2.1 实例与运行

- 模板描述启动内容；实例代表一个受管理房间；运行代次代表一次进程运行。
- 每次新建意图产生独立实例 ID，并保存配置快照。模板修改不影响已有实例；模板路径、地图名、hostname、PID 和端口均不能单独充当实例身份。
- 每实例独立管理进程、端口、cfg、日志和状态；允许共享 VPK。
- 实例为临时房间：创建即请求启动；主动停止并确认进程退出后，自动回收活动实例及临时文件、解除端口分配。再次开房从模板创建新实例，不复用旧 ID。
- 重启保留实例 ID、配置快照和端口，内部停止不触发实例回收；确认旧进程退出后才能启动下一代。停止失败不得启动第二个进程；地图重载不算重启。
- 启动失败或进程意外退出不按正常结束自动回收；保留失败记录和日志，不自动拉起。启动超时报告失败并尝试停止，未确认退出时保留身份与端口分配。
- 停止房间无需考虑玩家状态：不检查人数、不等待玩家退出或对局结束，收到停止请求后进入进程停止流程。具体退出机制由引擎验证确定。
- 单实例操作失败不默认停止其他实例。

### 2.2 管理端与恢复

管理端由用户或上层程序显式启动并持续运行；CLI 连接管理端完成操作，CLI 退出不影响专服。

管理端退出默认保留专服。重新启动后必须核验进程身份再恢复管理，不能仅凭 PID 或端口接管或终止进程。该能力依赖平台验证；若无法可靠实现，应回到范围评审，不能静默改成“退出即杀服”。

同一 data-dir 只允许一个管理端写入。首版以一个管理端管理本机多个实例为标准部署方式；不同 data-dir 不提供跨管理端的统一端口预约保证，仍须检查实际绑定冲突。共享安装中的生成文件必须避免跨 data-dir 重名，不得清理其他管理端的文件。

### 2.3 状态与操作

进程存活、房间就绪、操作完成分别表达。端口开放不等于可以进房，旧日志不证明本次启动成功，证据不足返回未知。

首版至少区分以下维度，具体状态枚举在 M1-01 固定：

| 维度 | 最小表达 |
| --- | --- |
| 进程 | 未运行、运行中、未知 |
| 房间 | 未知、加载中、就绪、失败 |
| 操作 | 操作 ID、类型、执行中/成功/失败、失败阶段与原因 |

创建或重启操作仅在本轮达到约定的就绪条件后成功；进程已经启动但仍加载时，操作保持执行中。观察不足时返回未知，启动超时后按失败流程处理。操作成功是历史结果，不代表房间此后一直可用；进程退出或状态证据失效时必须更新当前状态，并标明证据时间与运行代次。

具体协议字段在实现时确定。就绪判定必须说明证据及适用地图；实际客户端连接用于验收，不能由 A2S 或日志命中代替。

## 3. 配置与文件

### 3.1 路径和存储

默认 data-dir 为核心可执行文件旁的 `data`，与终端当前目录无关；支持 `--data-dir` 指定绝对路径。不可写时明确报错，不自动换目录。管理端与 CLI 使用同一 data-dir。

用户提供的程序、工作目录、VPK 和目录路径均使用绝对路径。Dota 工作目录独立指定。已支持的 CLI 配置覆盖项优先于模板，但不回写模板。

建议运行数据布局：

```text
<data-dir>/
├── manager/                  管理端日志、元数据
├── history/                  已回收实例的结果与诊断档案
└── instances/<实例ID>/
    ├── config.json           配置快照
    ├── state.json            进程身份、运行代次、操作记录
    └── runs/<运行代次>/
        ├── engine.log
        └── startup.cfg       执行内容副本
```

内部文件格式可随实现调整。根据现有实验，实际 cfg 先生成到 Dota 的 `game/dota/cfg`，使用唯一文件名并记录归属；data 内副本不等于引擎读取位置。清理仅限已核验归属的文件，不覆盖用户文件。IPC 端点位置按平台选择。

### 3.2 模板

沿用 [Windows 示例](examples/template.windows.json) 的 JSON 内嵌 cfg 方案：

| 字段 | 含义 |
| --- | --- |
| schemaVersion | 模板格式版本，当前为草案 |
| name | 显示名，不是实例 ID |
| executable | 专服程序绝对路径 |
| workingDirectory | Dota 工作目录绝对路径 |
| arguments | 按顺序传递的参数数组 |
| cfg.directory | 引擎 cfg 生成目录 |
| cfg.lines | 按顺序执行的引擎命令 |

核心直接创建进程，不通过 shell 执行整段命令。模板视为受信任的本机配置，允许指定程序和引擎命令；本地 API 的访问限制是必要边界，首版不承诺安全执行不可信模板。

模板基线保留四个占位符：`instance_id`、`game_port`、`cfg_name`、`log_path`。实例 ID 由核心分配；cfg 文件名和日志路径按运行代次生成。未知占位符报错。必需位置、转义、重复引用和 CLI 覆盖冲突规则在模板实现前确定。

核心可校验声明的字段与受支持占位符，不承诺理解任意引擎命令。参数数组和 cfg 是不同语法，分别处理空格、引号和换行；不能共用 shell 转义规则。

示例保留 `sv_hibernate_when_empty 0` 和引擎默认 VAC 模式。这是示例约定，不向所有自定义模板强制注入或拦截。

配置快照只固定模板内容，不冻结 VPK、游戏版本或额外 `exec` 文件。验收记录应注明所用资源与版本，避免将快照等同于完整环境复现。

### 3.3 端口与资源回收

已确认采用统一分配：模板使用 `{{game_port}}`；创建时可通过 `--port` 指定端口，不指定则由核心从配置范围内自动选择。手动和自动端口均检查并记录归属，不允许受管理实例获得冲突分配。实际所需的 TCP/UDP 及附加端口由 M0 验证，不能假设每个实例只占一个端口。

- 端口在创建时确定，实例存续期间保持不变；重启沿用原端口，遇到外部占用时报错，不静默换端口。
- 分配记录只约束核心自身，不等于持续占用操作系统端口。预检后仍可能被其他程序抢占，必须检查实际启动与绑定结果。
- 主动 `stop` 确认进程退出后自动回收实例，解除端口分配；无需再执行独立删除命令。停止失败或进程状态无法核验时保留记录与分配，不误报回收完成。
- 回收只移除活动实例和已核验归属的临时 cfg 等文件，不删除游戏安装、共享 VPK 或源模板。历史日志、配置快照和终止结果独立保留，按保留策略清理；历史实例不能再次启动。
- 启动失败或意外退出的实例保留诊断记录及分配。用户可通过 `stop` 显式结束并回收，仍须确认没有属于该实例的存活进程。
- 进程退出与文件清理结果分开记录；文件清理失败须报告并保留待清理记录，不能使已退出的进程重新启动。恢复时能够继续未完成的回收。

日志容量、历史记录保留期限、磁盘不足处理在交付前明确。操作结果应在约定保留期内可查询，不能因活动实例回收而丢失停止结果。

## 4. 最小使用流程

以下为目标命令，尚无实现；M1-01 固定选项与输出契约。管理端与 CLI 应随单实例闭环一起实现最小版本，以便尽早验证真实调用流程。

```text
d2core serve
d2core check --template "<模板绝对路径>"
d2core create --template "<模板绝对路径>" --json
d2core list --json
d2core status <实例ID> --json
d2core operation <操作ID> --json
d2core logs <实例ID> --tail 100
d2core restart <实例ID>
d2core stop <实例ID>
```

`check` 只做静态检查，不启动 Dota，也不保证运行时端口可用或地图可加载。`create` 创建并请求启动，支持 `--port` 显式指定端口。首版不提供只创建不启动或独立 `start` 命令。

`list` 列出活动及失败待回收实例；`status` 在历史保留期内也可查询已回收实例的终止结果；`logs` 可读取保留的历史日志；`operation` 查询操作结果。创建返回实例 ID；创建、重启、停止返回操作 ID 及真实当前状态。已受理不能表述为已就绪。程序可选用 CLI 的 JSON 输出，常规集成使用本地协议。

管理端持续执行已受理操作，调用方断开不隐式停止专服；同一实例的变更操作串行处理。启动或重启加载期间收到 `stop` 时，应转入停止流程，不必等到房间就绪或加载超时；先阻止该操作继续创建下一代进程，再核验并停止已创建的进程。被中止的操作返回明确终态，具体枚举在 M1-01 定义。创建请求使用调用方提供的幂等键区分“重试”与“新建”，键的作用域、保存期限及不同内容复用同键的处理在 API 实现前确定。

首版不支持实例原地更新配置；结束旧实例后，用修改后的模板重新创建。`restart` 用于运行中的实例，不复活已回收实例；失败实例先显式回收再创建。重复 `stop` 在终止结果保留期内返回已有结果，未知 ID 或记录过期不能伪报成功。等待超时、取消及历史结果的查询方式随最小协议确定。

连接信息区分实际监听地址与客户端可达地址，不把 `0.0.0.0` 当成客户端目标地址。公网映射和客户端启动由上层负责。

## 5. 先验证的引擎问题

原稿记录了 Windows 单地图实验，尚不能据此承诺双平台或任意地图可用：

| 历史观察 | 新项目需要复验 |
| --- | --- |
| 原始 VPK 绝对路径配合 cfg 的 map 命令可加载，客户端达到 SIGNONSTATE_FULL | 用户提供的测试地图、当前游戏版本、客户端资源要求及实际进房 |
| `-dedicated`、`-allow_no_lobby_connect`、`-port`、`-con_logfile`、`+exec` 的组合可运行 | 最小必需参数、参数顺序、带空格路径及 cfg 执行顺序 |
| cfg 位于 `game/dota/cfg`，`+exec` 使用文件名 | 双平台目录、权限及共享安装时的文件隔离 |
| 日志出现 `ss_loading → ss_active`、`Host activate`；A2S 可返回目标信息 | 哪些信号能判断何种状态，如何关联本次运行，以及启动超时如何处理 |
| `-ip 127.0.0.1` 时曾出现 TCP 回环、UDP 全地址绑定 | 实际监听地址和全部端口，不按参数推断网络隔离 |
| 核验程序路径与启动时间后可终止进程、释放端口 | 优雅停止、强制停止、残留子进程、管理端退出后存活及恢复身份 |

每次复验保存：操作系统与引擎版本、地图/VPK 标识、安装配置改动、完整参数与 cfg、日志、连接结果和退出记录。首批部署还应确认游戏资源、Steam/登录状态、运行库及是否依赖客户端等运行前提。

已有测试安装的 `gameinfo.gi` 曾被修改，不能当作默认安装基线。缺少原始证据的结论需要复验。

**阶段出口：**若目标地图无法稳定启动、进房或退出，先解决运行前提或调整支持范围，再展开完整管理架构。

## 6. 可选 A2S 工具

A2S 不作为启动或就绪判定的必需依赖。配置命令独立于管理端与普通启动流程：

```text
d2core a2s enable --dota-dir "<游戏安装绝对路径>" --json
```

检查 `game/dota/gameinfo.gi`，在 `GameInfo` 根节点合并 `GMS/Advertise 1`；修改前备份并保留其他内容。

已有相同值返回已配置，不重复插入或备份；不同值、结构异常、权限错误或并发变更明确报告，不静默覆盖。写后重新读取验证，返回已配置、已插入、冲突或错误。

成功只代表配置写入。运行中的专服需重启并实测查询；普通创建与重启不修改或补回该配置。此功能在单实例闭环之后实现，不阻塞 M0/M1。

## 7. 开发里程碑与验收

不要求开工前定完全部契约；每阶段开始前，只确定会影响该阶段实现和验收的规则。以下均为未完成工作，历史研究不等于验收通过。

| 阶段 | 工作与先决条件 | 出口验收 |
| --- | --- | --- |
| M0 运行基线 | 使用用户提供的地图准备双平台环境；复验第 5 节；调查进程控制与恢复可行性；允许建立最小 Go 工程与验证工具 | 双平台分别保存完整启动、实际进房、停止和再次启动的证据；列明运行前提和已知限制 |
| M1 最小纵向闭环 | 基于 M0 验证工具整理正式 Go 实现；实现最小模板、实例快照、管理端、CLI、本地协议、显式端口、运行日志、基本状态和停止后回收 | 从 CLI 创建实例并启动；返回可查询的 ID；完成进房、重启和停止后回收；历史日志与结果可查；失败能定位阶段；重启不产生重复进程 |
| M2 管理可靠性 | 在 M1 基础上实现身份持久化、管理端互斥、恢复、操作串行与创建幂等；明确数据写入与损坏处理 | 管理端正常退出或崩溃后可核验存活进程；不误接管/误杀；重复请求不重复创建；写入中断或损坏记录不引发重复启动；回收中断后能继续处理 |
| M3 多实例与回收 | 自动分配端口、资源冲突检测、日志与历史保留策略、多实例回收 | 至少两个实例同时运行并分别连接；同模板与不同模板均测；停止回收或失败一个不影响另一个；文件不互相覆盖 |
| M4 双平台与交付 | 汇总各阶段 Windows 10、Ubuntu 24 实机验证；完善双平台模板、构建包、协议文档和启动器调用样例；完成独立 A2S 工具 | 干净环境按说明可用；双平台完成主要流程及恢复验收；外部程序可调用；A2S 工具通过备份、冲突、幂等和写入失败验证 |

M1 阶段显式传入 `--port`；自动分配在 M3 完成前不宣称可用。M1—M3 每阶段在双平台执行相应实机验收，日常测试以手动命令为主；本地协议保留最小调用样例，为后续启动器集成提供稳定入口。

“两实例”是最小隔离验收规模，不是容量承诺。目标并发数量与机器配置确认后另做容量测试。

各阶段按风险编写必要测试：配置与转义、状态转换、请求重试、并发操作、PID 重用、端口抢占、日志代次、数据损坏和恢复。自动测试使用临时目录与测试进程；实机 Dota 测试显式执行，不触碰在用专服。

交叉编译不代替实机测试。性能比较使用同平台、同引擎、同地图及相近负载，分别记录核心自身开销和专服 CPU/内存、启动耗时；有明显退化时先解释原因，再确定可接受阈值。

项目初期只需一个 Go module、一个 `cmd/d2core` 入口及按职责逐步增加的 `internal` 包，不预建完整空目录。测试随代码维护；协议文档和平台示例随可用接口补充。忽略运行数据、日志、VPK、本机研究资料和构建产物。

## 8. 新项目建立与 Git/GitHub 管理

### 8.1 交接内容

新项目从本任务书、`README.md` 和 `examples/template.windows.json` 开始。示例路径和地图只是占位参考，需要用用户提供的资源替换。

新项目默认名称 `dota2-arcade-dedicated-core`，最终 GitHub owner、仓库名、可见性和许可证由用户在建仓时确定，不擅自设为公开或选用许可证。Go module 路径随最终仓库地址确定。GitHub 信息未就绪时可先整理任务和实验环境，不因此阻塞运行验证。

### 8.2 仓库约定

- 从项目开始使用 Git。先提交任务书、示例和忽略规则；每个可独立审查的任务形成一次或数次提交，说明变更及验证结果，不等全部开发完再一次性上传。
- 使用主分支保存已验证成果；有实质改动的任务可使用短期分支。无需为个人开发强制引入复杂分支流程。
- 用户确定远端后，在任务或阶段完成时推送到对应 GitHub 仓库。代码提交、推送和发布版本分别记录；不将“已推送”当作“已验收”。
- 纳入版本管理：源码、测试、脱敏的示例、协议文档、复验结论、构建与 CI 配置。排除游戏安装、VPK、运行 data、本地配置、凭据、原始敏感日志和构建产物。
- GitHub 中的示例使用通用路径；测试结论可提交，机器地址、账号、访问凭据和未脱敏原始证据留在忽略目录。禁止上传整份游戏资源作为测试夹具。
- 有 Go 代码后增加 Windows/Linux 构建与必要自动测试；CI 不运行真实 Dota，不连接测试云服务器。双平台实机验收另有记录。
- 开发任务状态采用“未开始 / 进行中 / 阻塞 / 完成”；更新本任务书执行表，并附提交号及验收记录链接。未执行的测试明确记为未验证。

### 8.3 开始实机测试时需要用户提供

测试地图/VPK、本地与云端 Dota 安装路径、Ubuntu 的访问方式和可用测试目录/端口。Win10 为专服与客户端同机；云端仅确定运行 Ubuntu 专服，具体客户端位置和网络路径现场记录。Ubuntu 24 是原稿验收目标，测试前核对具体版本。

这些是测试输入，不是新的产品设计问题；缺少某平台输入时可以推进不依赖该平台的工作，但不能将双平台验收标为完成。

## 9. M0：运行与平台验证任务

允许建立最小 Go 工程和验证工具，不提前搭建完整管理架构。以下任务均未开始。

| ID | 工作与依赖 | 交付物 | 完成条件 | 状态 / 证据 |
| --- | --- | --- | --- | --- |
| M0-01 | 建立新仓库；整理第 8 节交接输入 | 初始提交、忽略规则、环境记录 | 三份交接材料可读，示例 JSON 可解析；无游戏资源或凭据入库；远端未确定时明确标记 | 未开始 |
| M0-02 | 依赖 M0-01，建立双平台实验基线并选择验证工具所需的 Go 工具链 | `docs/validation/environment.md`；忽略目录内的实际测试配置 | 记录系统/架构、Dota 版本、VPK 标识、安装改动、权限、客户端位置和网络路径；明确测试资源隔离范围 | 未开始 |
| M0-03 | 依赖 M0-02，复验最小引擎启动 | 双平台启动参数、cfg 样例和连接结论 | 用户提供的地图可加载，客户端实际进房；日志与当前运行对应；记录带空格路径和参数编码结果 | 未开始 |
| M0-04 | 依赖 M0-03，验证端口与状态信号 | 实际 TCP/UDP 端口表、就绪证据表 | 记录全部必要端口与监听地址；区分进程存活、地图加载和实际可连接；A2S 不可用时仍能描述状态边界 | 未开始 |
| M0-05 | 依赖 M0-03，验证进程控制和管理恢复 | 最小 Go 验证工具、双平台进程控制结论 | 测试正常退出、退出超时、必要时强制结束、残留子进程；父管理进程退出/崩溃后专服存活；重启管理工具能够核验身份，拒绝不匹配进程 | 未开始 |
| M0-06 | 依赖 M0-03—05，确定实现选择 | `docs/decisions.md`、`docs/validation/m0.md` | 确认并固定 Go 工具链、必要依赖、平台进程控制与本地传输方案；给出就绪与超时依据；双平台验证通过；未通过时记录阻塞原因并保持任务未完成 | 未开始 |

M0 实机记录按以下顺序执行，每个平台分别留证：

1. 记录环境、既有配置改动及测试资源，确认不会影响在用专服。
2. 用用户提供的 VPK、唯一 cfg 和日志路径启动；记录 PID、可核验身份、开始时间及套接字。
3. 观察加载过程，由客户端实际进房；记录客户端连接结果及对应服务端日志。
4. 发起停止，不等待玩家状态；记录退出方式、耗时、残留进程和端口释放情况，再用同一配置启动验证可重复性。
5. 使用验证工具模拟管理端正常退出与崩溃；检查专服是否存活，以及重新运行工具后的核验结果。身份不匹配不得尝试终止。
6. 在隔离范围内测试端口被占用、无效资源、带空格路径和日志不可写，记录可识别的失败证据。

每条记录包含任务 ID、系统与版本、步骤、预期/实际结果、证据位置和结论。原始证据放忽略的 `local/validation/` 或其他明确路径；仓库提交脱敏结论与可复现步骤。

M0 出口：两平台均完成启动、进房、停止与再次启动；恢复机制有可验证方案。存在引擎阻塞时先解决运行前提；如需改变已确定行为，记录问题并与用户讨论，不静默降级。无需承诺任意地图兼容或预先确定容量上限。

## 10. M1：最小可用闭环任务

### 10.1 最小契约基线

- CLI 与本地 API 共享同一管理逻辑。默认变更请求返回受理结果及操作 ID，通过 `operation` 查询最终结果；CLI 返回受理成功不代表地图就绪。
- JSON 输出与诊断日志分离；错误至少含稳定错误码、失败阶段、可读原因，以及已经产生的实例/操作 ID。提交前的校验失败无需创建实例；创建记录后启动失败必须返回可追踪 ID。
- 实例至少区分活动、失败待回收、已回收；该维度与进程、房间及操作状态分开。已回收记录只供查询，不能启动或重启。
- 同一实例同一时刻只执行一个变更操作；除第 4 节规定的 stop 中止启动/重启外，M1 可对并发冲突明确返回忙碌，不能产生重复进程。完整创建重试幂等和崩溃一致性在 M2 验收。
- 停止与回收分别报告。确认进程退出前不释放实例分配；清理失败保留可重试的清理记录及历史结果，不把停止失败当作已回收。
- 首版默认以同一操作系统用户调用为实现起点，必须限制其他用户访问；若实测需要不同服务账户，先记录访问模型再扩展，不开放无保护端点。
- 未支持的 schemaVersion 或不兼容数据格式明确拒绝，不覆盖旧数据。模板检查不依赖运行中的管理端。
- 模板版本、持久化格式版本、本地协议版本及程序版本分别识别。不同版本 CLI/管理端不兼容时明确拒绝；发布包能报告程序版本和构建来源，便于复现实验与定位问题。

具体字段、退出码、超时配置名和 IPC 编码在 M1-01 形成单一协议文档，任务书不重复维护其全部细节。

### 10.2 执行任务

| ID | 工作与依赖 | 交付物 | 完成条件 | 状态 / 证据 |
| --- | --- | --- | --- | --- |
| M1-01 | 依赖 M0-06，固定最小契约 | `docs/local-api.md`、命令输入/输出样例、状态转换表 | 覆盖 serve/create/list/status/operation/logs/restart/stop/check；明确忙碌、停止中止加载、未知 ID、历史查询、协议版本和受理与完成区别；示例与模板一致 | 未开始 |
| M1-02 | 依赖 M1-01，实现模板校验与展开 | 配置模块、边界测试、双平台示例 | 绝对路径、未知占位符、格式版本、参数数组与 cfg 转义有明确结果；不执行 shell；不修改源模板；失败不启动进程 | 未开始 |
| M1-03 | 依赖 M1-02，实现实例与运行记录 | ID、快照、运行目录、cfg 归属与显式端口记录 | 同模板创建两份记录不覆盖；每次运行使用独立日志/cfg；冲突端口拒绝；临时文件仅清理自身生成内容 | 未开始 |
| M1-04 | 依赖 M0-05、M1-03，实现进程启动和观测 | 平台进程模块、状态观测和日志读取 | 正常启动、早退、加载超时均有真实状态；旧日志不触发就绪；超时尝试停止，不确认退出则保留身份 | 未开始 |
| M1-05 | 依赖 M1-01、M1-04，接入最小管理端与 CLI | 本地传输、端点发现、访问限制、操作查询 | 同 data-dir 第二管理端明确失败；CLI 退出不停止专服；变更调用可查询结果；JSON 可解析，日志不混入 | 未开始 |
| M1-06 | 依赖 M1-03—05，实现重启、停止与回收 | 生命周期操作、历史查询、清理重试记录 | 重启保持 ID/端口且不重叠运行；加载期间 stop 可中止；stop 无玩家等待；退出确认后回收；停止失败不释放分配；重复 stop 可查已有结果 | 未开始 |
| M1-07 | 依赖 M1-02—06，完成双平台闭环验收 | `docs/validation/m1.md`、构建与测试记录 | 以下完整流程与失败用例通过；已知限制明确；成果提交并同步已配置的 GitHub 远端 | 未开始 |

M1 手动验收流程：启动管理端 → check 模板 → create 指定端口 → 查询操作与实例状态 → 客户端进房 → restart → 再次进房 → stop → 查询历史结果和日志 → 确认活动列表移除实例、端口分配解除 → 从同一模板创建新 ID。验证结束后回收测试实例。

必要失败用例：无效模板不启动；端口占用明确失败；启动早退与超时保留诊断；加载或重启期间 stop 后不再派生新进程；历史成功不掩盖后续进程退出；停止未确认不回收；文件清理失败可定位；已回收 ID 不可 restart；未知 ID 不伪报成功；共享 VPK/模板未被删除。进程身份冲突和不退出场景优先用测试进程模拟，避免破坏真实游戏环境。

M1 只是可用性里程碑，不是完整交付。管理端崩溃后的完整恢复、创建请求幂等、自动端口分配、多实例隔离和存储上限仍按 M2—M4 完成后才能宣布首版交付。

## 11. 后续阶段的决策与完成规则

以下由执行者在对应任务开始前确定并记录，不阻塞新项目开工；涉及产品范围变化时再与用户讨论。

| 决策 | 最迟阶段 | 确定依据 |
| --- | --- | --- |
| 就绪证据、启动/停止超时、强制退出方式 | M0-06 / M1-01 | 双平台实测，不要求用户预选底层实现 |
| 占位符具体位置、覆盖冲突、错误码与状态字段 | M1-01 | 现有 JSON 示例及本任务书行为约束 |
| 持久化原子性、恢复核验、创建幂等键作用域和期限 | M2 开始前 | 请求重试、管理端崩溃和部分写入场景；同一键不同内容必须冲突，实例回收后保留期内的旧键不得创建新房间 |
| 自动端口范围、附加端口分配策略 | M3 开始前 | 实测端口需求与测试机器可用范围；可配置，默认值写入文档 |
| 日志及历史容量、期限、磁盘不足处理 | M3 开始前 | 有限存储、诊断需求、操作结果与幂等记录的保留一致性 |
| 容量测试规模、机器配置、性能阈值 | M3 容量测试前 | 先满足两实例隔离，再与用户确定实际目标，不阻塞单实例开发 |
| 安装与地图更新流程 | M4 前 | 上层负责更新；核心不更新共享资源，文档说明更新前的停止与回收步骤 |

M2 必测崩溃窗口：启动意图已记录但进程未创建、进程已创建但身份记录尚未完成、终止结果已记录但回收未完成。设计必须说明如何发现并核验可能残留的进程；无法确认时标为待核验并阻止重复启动，不通过再次 create 掩盖问题。另测整机重启后的旧 PID/记录，不得接管无关进程或自动拉起房间。

M3 在无自动端口可用时明确报错；并发创建和回收交错时不得重复分配。M4 的启动器样例必须覆盖请求超时后使用同一幂等键重试、查询就绪、读取连接信息、停止后查询结果的完整流程。

每个阶段在开始时按 M0/M1 的格式拆分任务，完成后记录命令、结果、未验证项和提交号。正常 Git 提交和推送跟随开发持续进行；完整发布须通过 M0—M4，提供双平台可执行文件、模板、操作说明、本地协议、启动器调用样例及已知限制。

新项目首次执行顺序：读取本任务书与示例 → 核对项目目录及仓库信息 → M0-01 建仓 → 收集测试输入 → 依序执行 M0。不要把历史观察当成新项目验收，也不要跳过 M0 直接实现全部管理功能。
